# Frontend – ortam değişkenleri (Vite: VITE_ ile başlamalı)
# Bu dosyayı .env olarak kopyalayın.

# Backend API adresi. Boş bırakırsanız dev proxy (localhost:3001) kullanılır.
# Vercel'de deploy ederken genelde boş bırakın (aynı origin).
# VITE_API_URL=http://localhost:3001
